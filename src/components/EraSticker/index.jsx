import styled from "styled-components"

const Container = styled.div`
    width: 50vw;
    position: fixed;
`
const EraSticker = () => {
    return <Container>

    </Container>
}
export default EraSticker